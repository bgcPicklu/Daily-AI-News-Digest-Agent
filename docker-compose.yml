version: '3.9'

services:

  news-agent:

    build: .

    container_name: news-agent

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./data:/app/data

    restart: always